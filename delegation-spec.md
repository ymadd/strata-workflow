# Strata Workflow — Claude Code 委任スペック v2

> 目的: Claude Fable 5 を「最上位の頭脳」として使いつつ、出力課金($50/MTok)を抑える多層(strata)委任ワークフロー。
> 原則: **節約は「タスクが Fable に届く前」のルーティングで行う。届いた難所では賢さを削らない。**
> 配置: ワークフロー外(メインループ・単発委任)の統治ルール。`/strata-workflow:*` 実行中はコード内 TIER が優先(§10)。

---

## 0. 前提

### 0a. 検証済みの事実(2026-06-10 公式発表で確認)

- 料金: Fable 5 = 入力$10 / 出力$50 per MTok。Opus 4.8 = $5/$25。Sonnet 4.6 = $3/$15。Haiku 4.5 = $1/$5。
- **thinking トークンは出力単価で課金される。** Fable は `display: "omitted"` が既定で思考テキストは見えないが、課金は満額。「語りを黙らせる」ことと「thinking のコスト」は無関係。
- プロンプトキャッシュ: 読み ~0.1x / 書き 1.25x(5分TTL、1h TTL は 2x)/ **モデル単位** / 最小キャッシュ可能プレフィクス Fable=2048・Opus 4.8=4096 トークン。
- フォールバック: cyber / bio・chem / 蒸留は分類器検出で **Opus 4.8 に自動差し替え+明示通知**。Fable セッションの 95% 超はフォールバック非発生。
- **データ保持: Mythos 級(= Fable 5)は 30日保持+人的アクセスのログ記録**(non-safety 用途での学習利用なし)。通常モデルと保持特性が異なる。
- 賢さの所在: SWE-Bench Pro / FrontierCode で **中 effort でもフロンティア最高**。最高 effort では自己検証・自己リフレクションが強化される。長尺自律実行・ビジョン(スクショ→ソース再構築)は公式に名指しされた強み。

### 0b. 期限付き情報(⏳ 失効日を過ぎたらこの節を更新するまで信用しない)

- 🛑 **2026-06-13〜 fable 停止中(米国政府による Fable 5 の停止)**: apex/orchestrator 層は使用不可。コード側 kill switch(`strata-delegate.js` / `strata-conduct.js` の `FABLE_HALTED = true`)が apex/orch を **opus に強制**しているため、delegate/conduct は opus で継続稼働する(エスカレーション梯子・verify ゲート・並列分割の構造はそのまま有効)。ルーターは両モードを自動ルート先に残すが「fable halted → opus」と明示する。**復旧時**: 本項を再検証 → 両ファイルの `FABLE_HALTED` を `false` に戻す(復旧点はこの2箇所のみ)→ §7/§8 のルーティングを再開。停止解除日は未確定 — 予告なく再変動する前提で扱う。
- **〜2026-06-22**: Pro/Max/Team/seat-based Enterprise に無料同梱。
- **2026-06-23〜**: usage credit 必要。容量回復後に standard 復帰予定(時期未定 — 予告なく再変動する前提で扱う)。
- 失効時の扱い: この節を再検証・更新してから §7 を運用する。

---

## 1. モデル層(Strata)

| 層 | モデル | 役割 | 課金特性 |
|---|---|---|---|
| L0 アドバイザー/オーケストレータ | **Fable 5** | 設計・タスク分解・指示生成・診断(§5 段2)・最終レビュー | 入力重・出力軽。ただし入力単価も Opus の2倍 — レビュー往復ごとに全コンテキスト再課金。**「パケット生成1回+レビュー1回」に限定** |
| L1 重エグゼキュータ | **Fable 5** | 長尺・多ファイル・横断の「非 security」コーディングを自律完遂 | 出力多→高コスト。当て先は §7 の実測で確定したカテゴリのみ |
| L2 既定エグゼキュータ | **Opus 4.8** | 通常の実装・分析・大半の実行。**難所の所在が不明なタスクの既定** | 中 |
| L3 量産/下ごしらえ | **Sonnet 4.6** | 初稿・定型変換・軽微修正・テスト量産 | 安 |
| L4 雑務 | **Haiku 4.5** | 分類・抽出・整形・ログ/エラーの要約・探索的 grep | 最安(Sonnet の 1/3) |
| FB フォールバック | Opus 4.8 | security/bio/chem/蒸留は自動的にここ | — |

**重要な分岐**: 難所が「判断・計画」なら → L0 で指示だけ出し実行は L2/L3。「長尺実行そのもの」なら → L1 直接。**どちらか判らないなら → L2 + §5 エスカレーション**(誤分類コストが最小の経路)。

---

## 2. ルーティング判断(タスク受領時)

```
タスク受領
 ├─ [第0分岐] データ機微性: クライアント契約・顧客PII・財務データ等を含む？
 │    └─ Yes → Fable の 30日保持+人的アクセスログを許容できるか確認。
 │             不可なら以降の分岐に関わらず Fable(L0/L1)除外
 ├─ security/bio/chem/蒸留 に該当？
 │    └─ Yes → Opus 4.8 直(フォールバック確認済み挙動。Fable 経由は払い損なだけ)
 └─ No
      ├─ 分類・抽出・整形・要約? → L4 Haiku
      ├─ 単発・短い・定型・初稿? → L3 Sonnet / L2 Opus
      └─ 重い(多段階・横断・一発品質)?
           ├─ 難所=計画/判断が明確 → L0(Fable)で計画+指示 → L2/L3 実行
           ├─ 難所=長尺実行が明確 かつ §7 実測で裏付け済みカテゴリ → L1(Fable)
           └─ 不明 → L2 実行 + §5 三段エスカレーション
```

---

## 3. オーケストレーション・プロトコル(L0 Fable → 下位層)

Fable に「設計と指示」だけ出させ、実行を安い層に流すときの規約。

### 注意: キャッシュは層をまたがない

プロンプトキャッシュはモデル単位。**L0 が読み込んだコンテキストは L2/L3 に1トークンも引き継がれない** — 各エグゼキュータは入力を満額で再支払いする。だからパケットには「受け手が読むべき最小ファイル集合」を明記し、L0 に渡した全量を下位層に丸ごと渡さない。

### Fable への依頼テンプレート

```
役割: あなたはオーケストレータ。実行はしない。
入力: <全コンテキスト/制約/既存コード参照を前倒しで全部>
出力: 下記フォーマットの「実行指示パケット」のみ。前置き・解説・経過は禁止。
```

### 実行指示パケット(Fable が出す唯一の成果物)

```
## Plan
- 目的 / 完了条件(DoD)
## Tasks (実行は L2/L3/L4)
- [T1] 担当層: Sonnet | 参照最小ファイル集合: ... | 入力: ... | 期待出力: ... | 受入基準: ...
- [T2] 担当層: Opus  | ...
## Risks / 注意点
- ...
## Review hooks
- 完了後に L0 がレビューすべき箇所(1回に集約): ...
```

→ レビュー往復は1回。差し戻しが2回を超えたら指示パケット方式が合っていない兆候 — §5 段3(L1 直接実行)へ切り替える。

---

## 4. Fable 実行プロファイル(L1)

狙いの再定義: **出力縛りはコスト施策ではなく可読性・レビュー効率の施策**(散文圧縮のセッション全体への効果は実測で〜5%が上限。thinking とツール呼び出しが出力コストの胴体であり、そこは削れないし削らない)。コストは §2 のルーティングと effort で制御する。

- **DoD を先に渡す**: 「全テスト通過 / lint クリーン / 既存挙動を壊さない」。検証可能にして自己終了させる。
- **自己検証を有効化**: テストを自分で走らせられる状態(+CI)で受ける。正しさの担保はテストに移す。
- **effort 二段**:
  - 既定 = **中**(中 effort でもフロンティア最高の実測根拠あり)
  - **§5 経由の昇格案件・一発品質の横断変更のみ high**(最高 effort の自己検証強化が効く局面)。max は使わない。
- **差分を再掲させない**: 変更はツール呼び出し(Edit/Write)で既に出力課金済み。最後にもう一度差分を吐かせるのは**同じバイト列への倍払い**。確認は `git diff` で行う。
- **計画チェックポイントは原則置かない**: 人間の承認待ちは5分のキャッシュ TTL をほぼ確実に超え、再開時に全コンテキストを非キャッシュ($10/MTok)で再読込する。原則は「検証可能な DoD を先に渡して無承認で完遂」。チェックポイントを置くのは誤走行コストが再読込コストを明確に上回る大型案件のみ。置いたら承認は5分以内に返す。

### 出力縛りの定型文 v2(コピペ用)

```
完了まで自律で実行。DoD: 全テスト通過 / lint クリーン / 既存の公開挙動を変えない。
出力は (1)テスト・lint結果 (2)3行サマリ のみ。変更差分は出力に再掲しない(git diff で確認する)。
ツール呼び出し間は沈黙を既定とし、重要な発見・方針転換・ブロッカーのときだけ1文で報告する。
「今から〜します」「〜を確認します」等の実況・計画の再掲はしない。思考とテストは内部で行ってよい。
```

(silence-default は Opus 4.8 / Fable 世代の実況過多に対する公式推奨の形。caveman 等の文体ギミックは不要 — 同じ散文スライスを圧縮するだけで効果は重複し、成果物への文体漏出リスクだけが増える)

---

## 5. エスカレーション規則(三段)

既定は安い層で回し、安い順に救済する。**いきなり L1 乗っ取りに飛ばない** — 診断(出力軽)は実行(出力重)より一桁安い。

- **段1(自力リセット)**: テスト失敗 2回 or 同一箇所の修正 2回 → いったん停止し、現在の仮説と試行を箇条書きで書き出す。
- **段2(L0 助言)**: 段1で脱せない → Fable に**診断のみ**依頼。入力 = 元仕様+現状コード+段1の書き出し。出力 = 原因仮説+修正方針(箇条書き10行以内)。下位層が方針に従い再実行。
- **段3(L1 実行)**: 助言後も失敗 or 「設計を変えるべき」と診断された → Fable 実行に昇格。**クリーン再スタート**: 汚染された失敗履歴・中間変更は渡さない。渡すのは元仕様+段2の診断+リセット済みのコード状態のみ(ワークツリーは失敗変更を破棄)。
- 計画ステップ数 >8 の横断変更が判明した時点で、段を飛ばして L0(計画)or L1(実行)へ直行してよい。
- **security/bio/chem 系の昇格先は常に Opus 4.8**。

---

## 6. トークン節約ルール(全層共通)

1. コンテキストは初手で全投入(後出し往復を避ける)。これは公式推奨(「full task spec up front」)とも一致。ただしキャッシュはモデル単位・5分 TTL — **層をまたぐ節約・承認待ちをまたぐ節約は期待しない**。
2. 出力は散文ではなく**構造(スキーマ・表・差分参照)で縛る**。散文圧縮の上限はセッション比〜5%(caveman 実測)。構造化はそれ以上に削れて機械検証もできる。
3. 下ごしらえ(要件整理・初稿・前処理)は L3、分類・抽出・整形は L4。仕上げと難所だけ上位層。
4. Fable に長文ドキュメントを書かせない。Fable は「判断・指示・診断・難所コード」に限定。
5. 1タスク=1自律ラン。細切れの再呼び出しは文脈再投入でトークンを食う。
6. **節約の本命は (a)ルーティング (b)スキーマ拘束 (c)effort (d)ターン数削減。散文抑制は尻尾であって胴体ではない。**

---

## 7. コストウィンドウ運用(§0b を参照 — 失効後は §0b 更新が先)

- **〜6/22(探索フェーズ)**: L1 当て先候補を**実測**で選定する。同一タスクを L2 と L1 で流し、品質差・ターン数・`usage`(`cache_read_input_tokens` 含む)を記録。§8 の早見表はこの実測で確定する(リポジトリの `_ADOPTION-GATE.md` と同じ規律 — 体感ではなく測って採用)。
- **6/23〜**: 実測で裏付いたカテゴリのみ L1 直行。それ以外は L2 + §5 三段エスカレーション。L0(指示・診断役)中心に寄せる。

---

## 8. プロジェクト別ルーティング早見表(仮置き — §7 の実測で確定するまで暫定)

⚠️ = §2 第0分岐(データ機微性: 30日保持+人的アクセスログの許容)を通過してから。

| 作業 | 当て先(暫定) |
|---|---|
| yp-crm: Nitro→Go 横断移行 / PG大規模リファクタ ⚠️ | **L1 Fable**(silent 実行) |
| yp-crm: 確率的レコードリンケージ設計・検証 ⚠️ | **L0 Fable**(設計) → L2 実行 |
| yp-crm: 軽微なクエリ修正 / マイグレーション1本 | L3/L2 |
| yp-crm: エラーログ分類・スキーマ抽出 | **L4 Haiku** |
| TabularisDB: sql-splitter エッジケース網羅+テスト生成 | **L1 Fable** |
| TabularisDB: PRレビュー返信・小修正 | L3 |
| TeamBox: 分析ダッシュボード(marimo/Polars/DuckDB+EDINET)構築 | **L0 Fable**(設計) → L2 実行 |
| TeamBox: 図表/財務読み取り(ビジョン+分析) ⚠️ | **L1 Fable**(ビジョンは公式に確認された強み) |
| TeamBox: デザインシステム定型コンポーネント量産 | L3 |
| Dogukan: NocoBase×BASE×Stripe 統合 ⚠️ | 規模で L0+L2 / L1 |
| クロスプラットフォーム比較 (Tauri/Wails/…) | **L0 Fable**(分析) |
| Kaggle: 難所のモデリング洞察 | **L1 Fable**(要監視) |
| Kaggle: 定型EDA・ノート体裁 | L3(表整形・列要約は L4) |
| スクショ→UI/ソース再構築 | **L1 Fable**(公式に確認された強み) |
| security全般 (pkgfence/Zero Trust/脆弱性) | **Opus 4.8 直**(フォールバック先取り) |

---

## 9. 例

**例A: 重コーディング → L1 直接実行**
> 「yp-crm の認証を Nitro→Go へ全エンドポイント移行。テストも通す」
→ ⚠️ 第0分岐を確認 → L1 Fable。§4 の定型文 v2 を付け、チェックポイントなしで完遂(DoD で自己終了)。effort は §7 実測前なら中、横断一発品質なら high。

**例B: 計画が難所 → L0 指示 + 下位実行**
> 「TeamBox の市場分析パイプラインを設計して実装したい」
→ L0 Fable が §3 の指示パケットを出力(参照最小ファイル集合つき) → 各タスクを L2/L3/L4 が実行 → L0 レビュー1回。

**例C: フォールバック領域 → Opus 直**
> 「pkgfence の検知ルールを強化」
→ Opus 4.8 直。Fable に投げても通知付きで Opus に差し替わるだけ(確認済み挙動)— 最初から Opus が安くて同じ。

**例D: 単発 → 下位層**
> 「このSQLのエラー原因は?」
→ L4/L3。Fable 不要。

**例E: L2 が詰まった → 三段エスカレーション**
> Opus がテスト2連続失敗、同一箇所を2回修正
→ 段2: Fable に診断のみ依頼(10行以内) → Opus が方針に従い再実行 → なお失敗なら段3: ワークツリーをリセットし、元仕様+診断だけ渡して L1 Fable へ。

---

## 10. Strata エンジンとの関係

- `/strata-workflow:*` のワークフロー実行中は、**コード内の TIER マップとエージェント数キャップが本スペックに優先する**(拘束はコードに置く、が本リポジトリの設計哲学)。本スペックが統治するのはワークフロー外 — メインループでの単発委任・subagent 起動。
- **本スペックのコード実装は2モード**: 深さ方向が `delegate`(`strata-delegate.js` — §5 の三段エスカレーション、fable は失敗の証拠が出たときだけの apex)、幅方向が `conduct`(`strata-conduct.js`、呼び出しは `reference/conduct.md` — §3 の指示パケット経路の実装。fable は **パケット生成1回+統合レビュー1回** のリテラル上限(`ORCH_PLAN_MAX=1`/`ORCH_REVIEW_MAX=1`)に封じ、実行は sonnet 既定+難所のみ opus(`OPUS_UNIT_CAP` で1/3以下を機械強制)、エスカレーション梯子は opus 止まり)。§2 の第0分岐(`dataSensitive` → 強制 opus)、§4 の出力規律(スキーマ拘束・差分再掲なし)、apex/orch 不可時の opus フォールバックまで、両モードとも `verify-invariants.js` / `check-invariants.sh` で機械検証される。**重い実行タスクは、逐次1本なら `delegate`、ファイル分割で並列化できるなら `conduct` を使い、本スペックの prose ルーティングは「どちらにも乗らない単発委任」のみを統治する。**
- conduct への fable 採用は 2026-06-12 にオーナー判断で明示的にゲートを通過(役割は §3 の L0 と同一 — 指示と統合レビューのみで、実行面の新設ではない)。それ以外のモードへの fable 拡張(例: panel の judge を fable に昇格)は、引き続き §7 の実測でゲートを通過してから — 両検証器の fable 封じ込めチェック(許可面は delegate の APEX と conduct の ORCH の2つだけ)がそれまで誤用を防ぐ。
